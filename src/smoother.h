#ifndef SMOOTHER_H
#include "simd.h"
#include "readout.h" //for the "peak_t" struct

static inline int32_t wrapidx(int32_t idx, int32_t n){
        while (idx >= n) {idx -=n;}
return idx;}














































#define SMOOTHER_H
#endif
